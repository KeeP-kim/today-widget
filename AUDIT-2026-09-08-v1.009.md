# v1.009 심층 점검 기록 — 2026-09-08

## 발견 및 수정

1. 공유 시세 수신 경로에서 급등 감지·깜빡임 누락: OnSharedQuote에서도 NoteSurge와 FlashSurge 호출. 같은 수신 시각은 다시 감지하지 않음.
2. 5분 시세 갱신이 통신 지연으로300초를 초과하면 무조건 무시: 설정 주기 +30초 허용, 긴 단절은 계속 제외.
3. 작은 코인 표시 반올림값으로 급등 판정: Quote.Value 원시 숫자 보존·사용. 출처/은행 변경은 기준을 다시 잡음.
4. 접힌 대표 시세의 애니메이션 대상 부재: 전용 Border 추가.
5. 예측 채점이 Math.Sign으로 미세 변동까지 방향 적중 판정: 기간별 보합 기준 ±0.1/0.3/0.5% 적용.
6. 반복·중첩 예측과 서로 다른 버전/자료를 성적으로 혼합: 동일 스냅샷의 기본/선택모델 쌍만 버전·출처별로 비교, 종료까지 겹치는 표본 제외.
7. 장마감 값이 새로 수신돼 최신 관측으로 처리: MarketClosed와 장마감 표시는 기록·채점에서 제외.
8. XML 한 건 손상 시 전체 집계 실패, 속성으로 파일명 조립: 크기/DTD/스키마/모델/기간/수치 검증, 손상/구형 기록 개별 제외. 저장 hit/error를 그대로 믿지 않고 원래 예측과 실제값에서 다시 계산.
9. 공유 시세가 렌더 시 갱신되며 모델 입력 기준값과 기록 기준값이 달라짐: 렌더 전에 입력 스냅샷 보존.
10. 이미 발생한 가격 상승 기사를 새 실적·수급 사건처럼 재가산: 가격 움직임만 있는 문장은 참고 맥락으로 분리.
11. 극단적 프롬프트가 최초 결론을 방어하게 유도: 강한 반박이면 결론 수정, 품목별 가격 경로로 설명하도록 변경.

## 확률 보정 기준

유사 패턴 빈도와 전체 빈도의 혼합 가중치를 이전에 결과가 확정된 표본만으로 선택한다. 학습30표본 뒤 시간순·겹침 제외 평가를 한다. 평가30표본 이상, 방향 적중수 비열화 없음, Brier 손실이 기존 방식과 전체 빈도보다 개선되고 차이의 보수적 오차여유를 통과한 경우에만 통계 보조값에 적용한다. 원시 과거 빈도와 미래 확률을 구분한다. 검증 대기/실패는 기존값 유지.

참고: https://scikit-learn.org/stable/modules/calibration.html (Brier와 calibration), https://otexts.com/fpp3/tscv.html (시간순 평가). 보정은 방향 적중률 향상 자체를 보장하지 않는다. 통계적 가드는 비정상성·모델 선택 위험을 없애지 못한다.

## 실제 공개 과거 자료 결과

2026-09-08 조회, 최종봉2026-09-07. 원시CSV는 저장소에 넣지 않은 작업 폴더에 저장. AI 호출 없이 시세만 수집했다.

| 품목 | 일간 평가 수 | 보정 Brier | 기존 Brier | 전체빈도 Brier | 보정/기존/전체 방향 적중수 | 적용 |
|---|---:|---:|---:|---:|---|---|
| DOGE(800봉) |209|0.6085|0.6093|0.6080|102/102/102|보류|
| BTC(800봉) |219|0.5613|0.5614|0.5587|101/103/104|보류|
| 코스피(759봉)|214|0.5101|0.5078|0.5065|126/126/133|보류|

주간의 순차 평가 표본은 DOGE2/BTC5/코스피19, 월간0으로 부족. 세 품목 모두 새 보정 적용을 정당화하지 못했다. 따라서 실제 적중률 향상을 달성했다고 보고하지 않는다. 합성 검증에서는 보정이 유효한 자료를 선택하고 무효·미래·중복·소표본은 거절하는지 검사한다.

## 남은 한계

실제 거래소 체결시각·휴장일 캘린더 보정 및 누락 만기 시세 보충은 남아 있다. 장마감/알 수 없는 시세는 수익률 성공 사례로 포장하지 않는다. 구형 기록은 보관하되 새 비교에서 제외. 시장 데이터 확장과 사건의 정량 surprise 수집은 PREDICTION-ROADMAP.md에 남아 있다.

## 최종 검증과 산출물

정상 회귀628개, 기록 채점17개, offscreen 레이아웃100개 통과. 사보타주 전체82/82 검출. 앞선 실행에서 소표본·보합 테스트가 결함을 놓치는 것을 확인해 반례를 강화한 뒤 전체 재실행했다. 이후 버전 및 평가0건 안내 문구 변경 후 정상 검사를 다시 통과했다.

별도 작업 폴더에서 -CI로 빌드, src28개 해시 일치. 루트 Onuln.exe/DLL 교체와 해시 확인 완료. 사용자 앱 재시작·실화면 알림 관찰은 수행하지 않았다. 합성 시세로 공유 수신/카드/접힌 화면 애니메이션을 확인했다. 산출물 SHA256과 남은 작업은 별도 기록에 남겼다.
